export const SCREEN_WIDTH = window.innerWidth / 2
export const SCREEN_HEIGHT = window.innerHeight / 2
export const FOCAL_LENGTH = 320 //distance from viewer to z=0
export const TRUE_ORIGIN = { x: SCREEN_WIDTH / 2, y: SCREEN_HEIGHT / 2, z: 0 };