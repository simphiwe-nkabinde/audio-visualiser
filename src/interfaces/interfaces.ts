export interface Position {
    x: number,
    y: number
}

export interface BubbleOptions {
    radius?: number;
    velocity?: Position;
}

export interface Dimensions {
    width: number;
    height: number;
}